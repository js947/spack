{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "spack test",
            "type": "shell",
            "command": "bin/spack -vd test extension",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}